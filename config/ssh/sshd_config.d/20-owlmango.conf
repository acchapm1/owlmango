# Installed to /etc/ssh/sshd_config.d/20-owlmango.conf by the installer.
# Allow password logins so owlmango machines are reachable over SSH out of the
# box. Key-based auth still works; root logins stay disabled.

PasswordAuthentication yes
KbdInteractiveAuthentication no
PermitRootLogin no
