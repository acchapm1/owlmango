# Faillock configuration
# Installed by dotfiles installer

# Disable account lockout
deny = 0

# Lock out period: 15 minutes (default: 900)
unlock_time = 900

# Failure interval: 15 minutes (default: 900)
fail_interval = 900
